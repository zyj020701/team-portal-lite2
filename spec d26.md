阶段二项目说明书：核心功能开发（Day 26）

一、项目目的

1.1 阶段目标

在阶段一已完成的 Monorepo 基建与设计系统基础上，完成 Team Portal Lite 最核心的业务功能开发，使项目从 "空架子" 变为 "可操作的工单工作台"。本阶段是整个 4 天实战中代码量最大、技术密度最高的一天，需交付五个可运行的核心模块：多租户主题系统、WebSocket 实时通知服务、虚拟滚动列表组件、工单列表页、工单详情页。

1.2 业务价值

多租户主题系统：让同一套前端代码服务于 5000 家不同品牌的企业客户，是 B2B SaaS 的核心商业化能力

实时通知：客服人员能第一时间响应客户投诉，是 "高级接单台" 区别于普通后台的关键体验

虚拟滚动列表：解决 10 万条历史工单的浏览器渲染瓶颈，是大数据量场景下的性能基石

工单列表与详情：构成客服日常操作的主流程，是整个系统使用率最高的两个页面

1.3 能力沉淀目标

通过本阶段开发，掌握运行时 CSS 变量换肤、WebSocket 断线重连与消息队列、虚拟滚动动态行高计算、复杂表单与状态机交互等前端高阶技术，这些均为大厂 P6/P7 面试中的高频考察点。

二、项目功能

2.1 多租户动态主题系统

租户识别与主题加载：根据当前登录用户所属租户，从配置中读取对应的品牌主题 Token（主色、辅助色、圆角风格等）

运行时 CSS 变量注入：将主题 Token 注入到 :root 或指定容器的 CSS 变量中，实现无刷新换肤

主题切换演示能力：提供一个租户切换器（开发环境用），可在 A 公司（蓝 + 白）与 B 公司（红 + 金）之间切换，验证主题隔离效果

暗色模式支持（可选）：在主题体系中预留暗色模式变量位，确保后续扩展不破坏现有结构

2.2 WebSocket 实时通知服务

连接管理：封装 WebSocket 客户端，支持自动连接、心跳保活（ping/pong）、指数退避重连

消息队列：建立客户端消息队列，新消息入队后按优先级处理，避免短时间大量推送导致渲染阻塞

通知中心 UI：顶部导航栏通知铃铛，显示未读计数；点击展开通知列表，支持标记已读、全部已读

桌面通知（可选）：调用浏览器 Notification API，在页面后台时弹出系统级通知

离线消息同步：重连后向服务端请求离线期间的消息，确保不遗漏

2.3 虚拟滚动列表组件

核心虚拟滚动：基于 @tanstack/react-virtual 实现，仅渲染视口内可见行，DOM 节点数控制在 20-30 个

动态行高支持：工单标题 / 摘要长度不一时，支持动态测量行高，滚动位置准确不跳动

滚动到指定项：提供 scrollToIndex 方法，支持从通知中心点击后跳转到对应工单

加载更多与上拉加载：滚动到底部时触发分页加载，与 TanStack Query 的 fetchNextPage 集成

空态 / 加载态 / 错误态：列表为空、加载中、加载失败三种状态均有对应 UI

2.4 工单列表页

工单数据展示：每行展示工单编号、标题、客户名称、优先级、状态、创建时间、处理人

多维度筛选：按状态（待处理 / 处理中 / 已解决 / 已关闭）、优先级（高 / 中 / 低）、处理人筛选

关键词搜索：支持按工单标题、客户名称模糊搜索，搜索防抖处理（300ms）

排序：支持按创建时间、优先级、状态排序，点击表头切换升序 / 降序

批量操作：支持多选工单后批量分配、批量关闭

分页与虚拟滚动结合：列表主体使用虚拟滚动，数据通过 TanStack Query 无限加载

2.5 工单详情页

工单信息展示：工单基本信息、客户信息、时间线（创建 / 分配 / 状态变更记录）

状态流转：实现工单状态机，待处理→处理中→已解决→已关闭，每个状态转换有对应操作按钮，非法转换自动禁用

内部备注：客服可添加内部备注，备注支持富文本或 Markdown（至少支持纯文本 + 换行）

优先级修改：可在详情页修改工单优先级，修改后列表页实时同步

处理人分配：支持将工单分配给指定客服，分配后触发 WebSocket 通知给被分配人

返回列表位置保持：从详情页返回列表时，保持之前的滚动位置和筛选条件

三、核心约束

3.1 性能约束

主题切换耗时 ≤ 50ms，切换过程中无白屏、无闪烁（FOUC）

WebSocket 消息从接收到 UI 展示延迟 ≤ 100ms

10 万条模拟数据下，虚拟滚动列表滚动帧率 ≥ 50fps，DOM 节点数稳定 ≤ 30

工单列表页首屏加载（含数据请求）≤ 2s（本地模拟环境）

搜索输入防抖 300ms，连续输入不触发频繁请求

3.2 代码质量约束

TypeScript 严格模式下 0 个 any，WebSocket 消息、工单数据、主题 Token 均有完整类型定义

所有自定义 Hooks（useTheme、useWebSocket、useVirtualList）必须有单元测试

主题系统中禁止硬编码颜色值，全部通过 CSS 变量或 Tailwind Token 引用

组件职责单一：虚拟滚动组件只负责渲染逻辑，不耦合工单业务字段

3.3 架构约束

WebSocket 连接逻辑封装在独立 Package（@team-portal/ws-client）中，不与 UI 组件耦合

主题系统逻辑封装在独立 Package（@team-portal/theme）中，包含 Token 定义、类型、切换 Hook

虚拟滚动组件封装在 UI 组件库 Package（@team-portal/ui）中，作为通用组件可被其他 App 复用

工单列表页与详情页的业务逻辑放在 apps/web 中，不污染共享包

状态边界严格：服务端工单数据走 TanStack Query，UI 状态（筛选条件、选中项、弹窗开关）走 Zustand

3.4 交互约束

所有异步操作（加载、提交、切换）必须有加载态反馈，禁止无响应的 "死点击"

状态流转操作需有二次确认（如关闭工单），防止误操作

键盘可访问性：列表支持上下键导航、Enter 进入详情、Esc 返回

错误处理：API 请求失败、WebSocket 连接失败均有友好提示与重试入口

四、技术限制

4.1 主题系统技术限制

必须使用 CSS 变量（Custom Properties）+ Tailwind CSS 变量映射 方案实现运行时换肤

不得使用 CSS-in-JS 运行时方案（如 styled-components 的 ThemeProvider 运行时注入）

不得为每个租户打包独立 CSS 文件，必须是一套 CSS + 运行时变量切换

Tailwind 配置中颜色必须引用 CSS 变量，如 primary: 'var(--color-primary)'

4.2 WebSocket 技术限制

必须使用原生 WebSocket API 封装，不得引入 socket.io-client 等重型库

重连策略必须为指数退避（1s → 2s → 4s → 8s → 最大 30s），不得固定间隔重连

心跳间隔固定为 30s，超过 60s 未收到 pong 则判定连接断开并触发重连

消息序列化格式为 JSON，禁止使用二进制协议（本阶段简化处理）

4.3 虚拟滚动技术限制

必须基于 @tanstack/react-virtual 实现，不得自行从零编写虚拟滚动逻辑

行高模式必须使用动态测量模式（dynamic），不得使用固定行高简化

列表容器必须有明确的固定高度或 flex: 1，不得依赖内容撑开高度

每条列表项必须有稳定的 key（用工单 ID），禁止使用数组索引作为 key

4.4 状态管理技术限制

服务端数据（工单列表、工单详情、用户信息）必须使用 TanStack Query 管理

客户端 UI 状态（筛选条件、搜索关键词、选中工单 ID、通知面板开关）必须使用 Zustand 管理

禁止在组件中用 useState 管理应全局共享的筛选条件（否则详情页返回后状态丢失）

TanStack Query 的 queryKey 必须包含筛选条件和分页参数，确保缓存隔离正确

4.5 框架限制

页面组件默认使用 React Server Component（RSC），仅在需要交互的子组件上加 "use client"

工单列表页的数据获取优先使用 RSC 首屏注水（initialData），后续交互通过 Client Component + TanStack Query 处理

不得在 RSC 中使用 useState、useEffect、事件处理函数

五、完成步骤

步骤 01：多租户主题系统开发（约 1.5 小时）

首先在 @team-portal/theme 包中定义主题类型（ThemeTokens 接口）与默认主题配置，包含颜色、圆角、间距等 Token。然后编写 ThemeProvider 组件，在应用初始化时根据租户信息将 Token 注入为 CSS 变量。接着在 Tailwind 配置中将颜色、圆角等字段映射为 CSS 变量引用，确保所有 Tailwind 类名能响应主题变化。最后开发一个租户切换演示组件，在蓝白主题与红金主题之间切换，验证换肤效果无闪烁、无遗漏。

验收要求：

需确认 @team-portal/theme 包中已定义完整的 ThemeTokens TypeScript 接口，至少覆盖主色、辅助色、语义色（成功 / 警告 / 错误）、中性色、圆角（三档）、间距（4px 基准网格）六个维度，且每个 Token 字段都有明确的类型约束，无 any。ThemeProvider 组件需能接收租户主题配置并将其注入为 :root 上的 CSS 变量，切换主题后通过浏览器 DevTools 检查 document.documentElement.style，确认变量值已实时更新。Tailwind 配置文件中，颜色与圆角字段必须引用 CSS 变量（如 primary: 'var(--color-primary)'），不得出现硬编码的十六进制色值。租户切换演示组件需支持在至少两套主题（A 公司蓝白、B 公司红金）之间一键切换，切换过程中无白屏、无闪烁（FOUC），切换耗时 ≤ 50ms。最终需对 Button、Input、Modal、Table 四个基础组件进行逐组件验证，确认所有颜色、圆角均跟随主题变化，无遗漏的硬编码样式。代码审查中若发现任一组件存在硬编码颜色值（如 #1677ff）或 padding: 8px 等字面量尺寸，即视为验收不通过。

步骤 02：WebSocket 实时通知服务封装（约 1.5 小时）

在 @team-portal/ws-client 包中封装 WebSocketClient 类，实现连接、断开、心跳、指数退避重连、消息发送等核心方法。然后编写 useWebSocket Hook，将连接状态、最新消息、未读计数暴露给 React 组件。接着实现消息队列：新消息推入队列后，通过 requestAnimationFrame 批量更新状态，避免高频推送导致的渲染风暴。然后开发通知中心 UI 组件（通知铃铛 + 下拉通知列表），集成未读计数、标记已读、全部已读功能。最后用 Mock 服务（或浏览器控制台模拟）测试消息推送、断连重连、离线消息同步三个场景。

验收要求：

需确认 WebSocketClient 类已实现完整的连接生命周期管理：connect() 建立连接、disconnect() 主动断开、send() 发送消息、onMessage/onOpen/onClose/onError 事件回调。心跳机制必须为每 30s 发送一次 ping，60s 内未收到 pong 则主动关闭并触发重连。重连策略必须为指数退避（1s → 2s → 4s → 8s → 最大 30s），并加入随机抖动（jitter），禁止使用固定间隔重连。useWebSocket Hook 需暴露 connectionStatus（connecting/connected/disconnected/reconnecting）、lastMessage、unreadCount 三个核心状态，且有完整的 TypeScript 类型定义。消息队列需验证：连续快速推送 100 条消息时，React 渲染次数远小于 100 次（通过 React DevTools Profiler 确认批量合并生效），页面无卡顿、无响应。通知中心 UI 需验证：铃铛图标显示未读计数（>99 显示 99+）、点击展开通知列表、单条标记已读后计数减 1、全部已读后计数清零且列表项样式变更。断连重连测试需验证：手动断开网络后连接状态变为 disconnected，恢复网络后自动重连并进入 connected，重连后请求并同步离线期间的消息，无遗漏。多标签页场景需验证：打开两个标签页，标记已读后另一标签页的未读计数同步更新（通过 BroadcastChannel 实现）。若重连为固定间隔、无心跳机制、或 100 条消息推送导致页面卡死，即视为验收不通过。

步骤 03：虚拟滚动列表组件开发（约 1.5 小时）

在 @team-portal/ui 包中基于 @tanstack/react-virtual 封装通用 VirtualList 组件，支持动态行高、滚动到指定项、上拉加载更多三个核心能力。组件需接收 items、itemKey、renderItem、onLoadMore、hasMore 等通用 props，不耦合工单业务字段。然后实现行高动态测量：使用 useVirtualizer 的 measureElement API，在每行渲染后测量实际高度并缓存。接着实现滚动到底部触发加载的逻辑，与 TanStack Query 的 fetchNextPage 对接。最后用 10 万条模拟数据测试：验证滚动流畅、DOM 节点数稳定、内存不持续增长、滚动位置准确不跳动。

验收要求：

需确认 VirtualList 组件封装在 @team-portal/ui 包中，Props 接口包含 items: T\[]、itemKey: (item: T) => string | number、renderItem: (item: T, index: number) => ReactNode、onLoadMore?: () => void、hasMore?: boolean、estimateSize?: number，且为泛型组件，不耦合工单业务字段。行高必须使用动态测量模式（useVirtualizer 的 measureElement），禁止使用固定行高简化。需准备 10 万条模拟数据（行内容长度不一，模拟真实工单标题长短差异），加载后验证以下指标：其一，DOM 节点数稳定在 20-30 个之间（通过 DevTools Elements 面板确认，滚动时不持续增长）；其二，滚动帧率 ≥ 50fps（通过 Chrome DevTools Performance 面板录制滚动过程确认）；其三，快速上下滚动无白屏闪烁、无内容错位；其四，动态行高下滚动位置准确，滚动条拖动后停在对应数据项，不出现跳动偏差。滚动到指定项功能需验证：调用 scrollToIndex(50000) 后，列表准确跳转到第 50000 条数据，位置无偏差。上拉加载功能需验证：滚动到底部时触发 onLoadMore，加载中显示加载态，加载完成后追加数据，hasMore 为 false 时不再触发加载，且加载过程中不会重复触发（有 isLoading 锁）。列表项的 key 必须使用业务唯一标识（如工单 ID），代码审查中若发现使用数组索引作为 key，即视为验收不通过。若 10 万条数据下滚动帧率 < 30fps、DOM 节点数持续增长超过 100、或滚动位置明显跳动，即视为验收不通过。

步骤 04：工单列表页开发（约 2 小时）

在 apps/web 中创建工单列表页，首屏通过 RSC 获取第一页数据并注水给 Client Component。然后编写筛选栏组件：状态筛选（下拉多选）、优先级筛选、处理人筛选、关键词搜索框（300ms 防抖）。筛选条件存入 Zustand store，确保页面跳转后返回时状态保持。接着将筛选条件映射为 TanStack Query 的 queryKey 和请求参数，实现筛选变更自动重新请求数据。然后集成虚拟滚动组件渲染工单列表，每行展示工单编号、标题、客户、优先级、状态、时间、处理人，并实现表头点击排序。最后添加批量操作栏：支持多选、批量分配、批量关闭。

验收要求：

需确认工单列表页路由为 /tickets，首屏通过 React Server Component 获取第一页数据并作为 initialData 注入 TanStack Query，避免客户端重复请求。筛选栏需包含四个维度：状态筛选（待处理 / 处理中 / 已解决 / 已关闭，支持多选）、优先级筛选（高 / 中 / 低）、处理人筛选（下拉选择）、关键词搜索（支持工单标题和客户名称模糊搜索）。搜索框需实现 300ms 防抖，连续输入不触发频繁请求，同时支持 Enter 键立即搜索（清除防抖定时器并立即触发）。筛选条件需同步到 URL query 参数（如 /tickets?status=pending\&priority=high\&keyword=xxx），刷新页面后筛选条件从 URL 恢复，且可分享带筛选条件的链接。TanStack Query 的 queryKey 必须包含所有影响数据的参数（\['tickets', 'list', { status, priority, assignee, keyword, page, sort }]），任何参数变更都生成独立缓存，筛选 A 与筛选 B 的数据互不污染。列表集成虚拟滚动组件，每行展示工单编号、标题、客户名称、优先级标签（颜色区分）、状态标签、创建时间、处理人头像或姓名，信息完整无遗漏。表头点击排序需验证：点击创建时间表头切换升序 / 降序，点击优先级表头按优先级排序，排序图标正确指示当前方向。批量操作需验证：勾选多行后出现批量操作栏，支持批量分配（选择处理人）和批量关闭（二次确认），操作成功后列表实时刷新（通过 invalidateQueries 触发），选中状态清空。返回状态保持需验证：从列表页进入详情页再返回，筛选条件、搜索关键词、排序方式、滚动位置均保持不变，不重置为默认状态。所有异步操作（加载、筛选、批量操作）必须有加载态反馈（骨架屏或 Spinner），禁止无响应的 "死点击"。若筛选条件未同步到 URL、queryKey 缺失参数导致缓存污染、或返回页面后状态丢失，即视为验收不通过。

步骤 05：工单详情页开发（约 1.5 小时）

创建工单详情页，通过动态路由 /tickets/\[id] 获取工单 ID，RSC 首屏加载工单详情。然后实现工单信息展示区：基本信息卡片、客户信息、时间线（创建 / 分配 / 状态变更记录）。接着实现状态流转功能：定义工单状态机（待处理→处理中→已解决→已关闭），根据当前状态渲染可用操作按钮，非法转换按钮禁用，关键操作（关闭工单）有二次确认弹窗。然后开发内部备注功能：备注列表展示 + 新增备注表单，提交后通过 TanStack Query 失效刷新详情数据。接着实现优先级修改与处理人分配功能，操作成功后触发 WebSocket 通知给相关人员。最后处理返回逻辑：从详情页返回列表时，通过 Zustand 恢复之前的筛选条件和滚动位置。

验收要求：

需确认工单详情页路由为 /tickets/\[id]，通过动态路由参数获取工单 ID，RSC 首屏加载详情数据并注水，Client Component 不重复请求（或设置合理 staleTime）。工单信息展示区需包含三个部分：基本信息卡片（工单编号、标题、状态、优先级、创建时间、处理人）、客户信息（客户名称、联系方式、所属企业）、时间线（按时间倒序展示创建、分配、状态变更、备注等事件，同一时间戳按类型优先级排序）。状态流转需实现明确的状态机：待处理 → 处理中 → 已解决 → 已关闭，每个状态下只渲染合法的操作按钮（如 "已关闭" 状态下所有操作按钮禁用），非法转换在提交前通过纯函数 canTransition(from, to) 二次校验，不依赖按钮禁用这一道防线。关闭工单等关键操作必须有二次确认弹窗（"确定关闭该工单吗？关闭后不可重新打开"），防止误操作。内部备注功能需验证：备注列表按时间倒序展示，每条备注显示操作人、时间、内容；新增备注表单支持多行文本，提交后显示加载态，成功后备注列表实时刷新（通过 invalidateQueries({ queryKey: \['tickets', 'detail', id] })），输入框清空。优先级修改与处理人分配需验证：操作成功后详情页信息实时更新，同时通过 WebSocket 向被分配人或相关人员推送通知（可在另一标签页验证通知到达）。返回逻辑需验证：从详情页点击返回或浏览器后退，列表页的筛选条件、搜索关键词、排序方式、滚动位置均精确恢复到进入详情页之前的状态，不重置。键盘可访问性需验证：详情页支持 Esc 键返回列表、Tab 键可聚焦所有操作按钮。时间线事件需来自统一数据结构（{ type, timestamp, operator, content }），合并后排序正确，无时间错乱。若状态机存在非法转换路径、关闭工单无二次确认、或返回列表后滚动位置丢失，即视为验收不通过。

六、常见陷阱

6.1 多租户主题系统陷阱

主题切换闪烁（FOUC）：CSS 变量注入晚于首屏渲染，导致用户先看到默认主题再跳到目标主题。对策：在 HTML 模板的 <head> 中内联一段同步脚本，在 React 挂载前就读取租户信息并注入 CSS 变量；或使用 RSC 在服务端渲染时就将变量写入 style 属性。

Tailwind purge 误删动态类：如果主题类名是动态拼接的（如 bg-${color}-500），Tailwind 的 JIT 编译器无法识别，会导致生产环境样式丢失。对策：永远使用完整的静态类名（如 bg-primary），颜色变化通过 CSS 变量驱动，而非动态拼接类名。

组件库样式不跟随主题：Radix UI 或 shadcn 组件的某些样式（如浮层背景、边框）写死了颜色，换肤时遗漏。对策：在设计系统阶段就将所有组件颜色 Token 化，开发主题切换后逐组件检查，可用浏览器取色器辅助验证。

CSS 变量继承问题：如果将变量注入到某个容器而非 :root，弹窗（Portal 到 body）会读不到变量。对策：主题变量统一注入到 :root（document.documentElement），确保所有元素包括 Portal 内容都能继承。

6.2 WebSocket 实时通知陷阱

重连风暴：网络不稳定时，固定间隔重连导致服务端压力骤增，且客户端耗电发热。对策：严格实现指数退避重连（1s→2s→4s→8s→最大 30s），并加随机抖动（jitter）避免大量客户端同时重连。

消息堆积导致 UI 卡死：短时间推送几百条消息，每条都触发 setState，React 渲染队列爆炸导致页面无响应。对策：建立消息队列，用 requestAnimationFrame 或 setTimeout(0) 批量合并更新；或在 Zustand 中用 setState 批量更新，减少渲染次数。

多标签页重复连接：用户打开多个标签页，每个标签都建一个 WebSocket，服务端连接数翻倍且消息重复消费。对策：使用 BroadcastChannel 实现标签页间通信，只让一个标签（主标签）维持 WebSocket 连接，其他标签通过 BroadcastChannel 接收消息；或使用 SharedWorker（兼容性稍差）。

心跳机制缺失：TCP 连接已断开但浏览器未触发 onclose（如网络切换、休眠唤醒），客户端以为还在线，实际收不到消息。对策：实现 ping/pong 心跳，每 30s 发送 ping，60s 内未收到 pong 则主动关闭连接并触发重连。

未读计数不同步：A 标签标记已读，B 标签的未读计数仍显示旧数字。对策：标记已读的操作通过 BroadcastChannel 广播给其他标签页，或在 TanStack Query 中用 queryClient.invalidateQueries 同步。

6.3 虚拟滚动列表陷阱

动态行高测量导致滚动跳动：行高是异步测量的，滚动时新行的高度还没测出来就用了估算值，导致滚动位置跳动。对策：给 estimateSize 一个合理的估算值（如平均行高 72px），测量后自动修正；使用 @tanstack/react-virtual 的 measureElement 而非手动 getBoundingClientRect。

滚动位置丢失：筛选条件变更或从详情页返回时，虚拟滚动容器重置到顶部。对策：将滚动位置存入 Zustand store，组件挂载后通过 virtualizer.scrollToIndex 或直接设置 scrollTop 恢复；注意要在数据加载完成后再恢复。

key 用索引导致渲染错乱：用数组索引作为列表项 key，数据增删后 React 复用错误的 DOM 节点，导致内容错位或状态混乱。对策：必须用工单 ID 等业务唯一标识作为 key，禁止用 index。

容器高度未设定：虚拟滚动依赖一个有固定高度的滚动容器，如果高度是 auto 或被内容撑开，虚拟滚动无法计算视口范围，会渲染全部行。对策：确保滚动容器有明确高度（如 height: calc(100vh - 200px) 或 flex 布局下 flex: 1; overflow: auto），并在开发时用 DevTools 确认容器高度生效。

上拉加载重复触发：滚动到底部时 onLoadMore 被连续调用多次，导致重复请求或页码错乱。对策：加一个 isLoading 锁，请求进行中不触发下一次；同时检查 hasMore 标志，没有更多数据时停止监听。

6.4 工单列表页陷阱

筛选条件与 URL 不同步：筛选条件只存在 Zustand 中，刷新页面后丢失，且无法分享带筛选条件的链接。对策：将筛选条件同步到 URL query 参数（用 useSearchParams），Zustand 作为客户端缓存，URL 作为真相来源；页面初始化时从 URL 读取筛选条件。

搜索防抖与立即提交冲突：用户输入后按 Enter 想立即搜索，但防抖 300ms 导致延迟感。对策：搜索框同时支持防抖自动搜索（输入停止 300ms 后触发）和 Enter 键立即搜索（清除防抖定时器并立即触发请求）。

TanStack Query 缓存污染：不同筛选条件的 queryKey 设计不当，导致筛选 A 的数据被筛选 B 复用，显示错误数据。对策：queryKey 必须包含所有影响数据的参数，如 \['tickets', 'list', { status, priority, assignee, keyword, page, sort }]，任何参数变更都生成新的缓存条目。

批量操作后数据不一致：批量关闭工单后，列表没有刷新，仍显示旧状态。对策：操作成功后调用 queryClient.invalidateQueries({ queryKey: \['tickets', 'list'] }) 触发重新获取；或使用乐观更新（optimistic update）先改本地缓存，失败再回滚。

6.5 工单详情页陷阱

状态机非法转换：用户通过 DOM 操作或快速点击绕过按钮禁用逻辑，执行了非法状态转换（如已关闭的工单重新打开）。对策：前端按钮禁用只是体验优化，状态转换的合法性必须在提交时再次校验（纯函数 canTransition(from, to)），且最终以后端校验为准。

返回列表滚动位置恢复时机错误：数据还没加载完就恢复滚动位置，导致恢复到错误位置或被重置。对策：在 TanStack Query 的 isSuccess 状态且列表数据渲染完成后，再调用滚动恢复；可用 useEffect 依赖 isSuccess 触发。

时间线排序混乱：工单的创建、分配、状态变更、备注等事件来自不同接口，合并后时间顺序错乱。对策：统一事件数据结构（{ type, timestamp, operator, content }），合并后按 timestamp 降序排列，同一时间戳的事件按类型优先级排序。

RSC 与 Client Component 数据重复获取：RSC 已经获取了详情数据，Client Component 又用 TanStack Query 请求一次，造成重复请求。对策：用 RSC 获取的数据作为 TanStack Query 的 initialData，并设置 staleTime 避免立即重新请求；或完全用 RSC 渲染静态部分，仅交互部分用 Client Component。

6.6 通用避坑建议

每完成一个模块立即自测：不要等到五个模块全部写完再一起测，模块间依赖复杂，晚测难以定位问题。主题系统测完再写 WebSocket，WebSocket 测完再写虚拟滚动，逐步推进。

Mock 数据要真实：用 Mock.js 或 Faker 生成接近真实的工单数据（长标题、不同长度的摘要、各种状态分布），不要用全是短文本的假数据，否则虚拟滚动的动态行高问题暴露不出来。

善用 React DevTools：开发过程中持续观察组件重渲染情况，Webpack 消息推送时如果整个列表页都在重渲染，说明状态管理粒度有问题，需要拆分 store 或用 useShallow。

预留联调时间：五个模块之间有依赖关系（如列表页依赖虚拟滚动和主题系统，详情页依赖 WebSocket 通知），模块间联调至少预留 30 分钟，不要把时间排满。



