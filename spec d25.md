任务说明书：Team Portal Lite 企业工单管理系统前端

一、项目目的

1.1 业务目标

构建一个面向大型企业客服部门的 B2B SaaS 工单管理系统前端界面（俗称 "高级接单台"），支撑超过 5000 家付费企业客户、累计 100 万客服人员 的日常工单处理，日活跃用户达 30 万（DAU 30w）。项目需复刻真实高并发、高可用的企业级 SaaS 工程环境，而非 Demo 级玩具项目。

1.2 个人成长目标

本项目作为简历核心项目，目标是打造 大厂 P6/P7 级敲门砖与创业 Lead 的核心竞争力筹码。最终需沉淀可量化成果：Lighthouse 性能分 96+、TypeScript 严格模式 0 Any、测试覆盖率 75%+，以数据证明工程能力。

1.3 核心价值定位

对业务：提供多租户隔离、实时响应、大数据量流畅操作的客服工作台

对工程：验证 Monorepo + RSC + 设计 Token 体系在大型 SaaS 场景下的可行性

对个人：形成从架构决策到工程落地的完整闭环能力证明

二、项目功能

2.1 多租户动态主题系统

支持按租户（企业客户）动态切换品牌主题，如 A 公司为蓝 + 白配色，B 公司为红 + 金配色

主题切换无需全局重新渲染，通过 CSS 变量 + Design Token 体系实现运行时换肤

每个租户可自定义主色、辅助色、圆角、间距等基础 Token

2.2 实时通知系统

基于 WebSocket 实现工单消息实时推送

客户发来投诉 / 新工单时，客服端立刻弹出新消息提醒，不依赖页面刷新

包含通知中心、未读计数、消息已读状态同步

2.3 大数据虚拟滚动列表

单个大型客户可能拥有 10 万条历史工单，需支持流畅浏览

采用虚拟滚动（Virtual Scrolling）技术，仅渲染视口内可见的几十条数据

滚动时动态加载 / 卸载 DOM 节点，确保内存占用稳定、不卡顿

2.4 响应式适配

客服在家用平板、在公司用大屏幕，界面均需自动适配

覆盖移动端（平板）→ 桌面端（大屏）的断点体系

布局、导航、表格密度随屏幕尺寸自适应调整

2.5 国际化（i18n）

界面支持 5 种语言：中文、英文、日文等

文案、日期格式、数字格式、RTL 方向（如涉及）均需本地化处理

语言切换不丢失当前操作状态

2.6 工单核心业务流程

工单列表展示、筛选、排序、搜索

工单详情查看与状态流转（待处理→处理中→已解决→已关闭）

客服分配、优先级标记、内部备注

工单统计概览 Dashboard

三、核心约束

3.1 性能约束

Lighthouse 性能分 ≥ 96：首屏加载、交互响应、累积布局偏移（CLS）均需达标

大数据列表滚动帧率 ≥ 50fps，10 万条数据下无明显卡顿

WebSocket 消息从接收到 UI 展示延迟 ≤ 100ms

3.2 代码质量约束

TypeScript 严格模式（strict: true）下 0 个 any 类型

测试覆盖率 ≥ 75%：单元测试（Vitest）+ 端到端测试（Playwright）

所有公共组件、Hooks、工具函数必须有类型定义和测试

3.3 工程规范约束

采用 Monorepo 架构，代码分层清晰，5 个 Apps + 10 个共享 Packages

Packages 之间禁止循环依赖

设计 Token 命名规则全局统一，禁止硬编码颜色 / 尺寸值

提交前必须通过 ESLint、Prettier、TypeScript 类型检查、测试全量通过

3.4 交付周期约束

总工期 4 天（Day 25-28），每天有明确阶段产出

Day 25 完成项目立项 + 架构设计（4 小时产出完整技术架构方案）

后续三天按阶段完成核心功能开发、联调、优化与部署

3.5 部署约束

部署至 Vercel，集成 Lighthouse CI 自动化性能检测

每次 PR 自动运行性能检测，不达标则阻断合并

四、技术限制

4.1 框架限制

必须使用 Next.js 14 + React Server Components (RSC) + App Router

不得使用 Pages Router 或其他 React 框架（如 Vite + React Router）

需充分利用 RSC 减少客户端 JS 体积，但实时交互部分需合理使用 Client Component

4.2 工程化限制

Monorepo 必须使用 pnpm + Turborepo，不得使用 Lerna 或单仓库结构

包管理严格使用 pnpm，不得混用 npm/yarn

构建缓存、任务编排通过 Turborepo 配置

4.3 UI 技术栈限制

组件库基于 Radix UI + Tailwind CSS + shadcn/ui

不得使用 Ant Design、Element UI 等重量级组件库

样式方案统一使用 Tailwind + CSS 变量，不得混用 CSS-in-JS（如 styled-components）

设计 Token 通过 Tailwind 配置 + CSS 变量双层管理

4.4 状态管理限制

客户端状态使用 Zustand，服务端状态使用 TanStack Query（React Query）

不得使用 Redux / Redux Toolkit

需明确区分客户端状态与服务端状态的边界，禁止用 Zustand 管理服务端数据

4.5 测试限制

单元测试框架 Vitest，E2E 测试框架 Playwright

不得使用 Jest + Cypress 组合

测试需支持 Monorepo 下的增量执行与覆盖率聚合

4.6 部署与 CI 限制

部署平台 Vercel，CI 集成 Lighthouse CI

不得自行搭建 Nginx/Node 服务器部署

环境变量、密钥管理通过 Vercel 平台配置

五、完成步骤

阶段一：项目立项 + 架构设计（Day 25，约 4 小时）

S1：任务：4 问开局定调：明确 Why（为什么做）、Who（给谁用）、What（做什么）、How（怎么做）

&#x20;       验收：需确认已产出一份完整的核心四问清单文档，其中 Why（为什么做）需明确说明项目的业务背景与个人成长目标，包括 B2B SaaS 定位、5000 家企业客户与 100 万客服人员的规模背景，以及简历核心项目的定位；Who（给谁用）需明确区分付费企业客户与最终客服用户两类角色，并说明各自的核心诉求；What（做什么）需列出项目必须交付的核心功能范围，明确多租户主题、实时通知、虚拟滚动、响应式、国际化五大核心挑战均在范围内；How（怎么做）需说明整体技术路线与 4 天工期的阶段划分。四个维度均需有具体结论，禁止出现 "待定"" 后续补充 " 等空泛表述。文档评审通过即为该步骤验收合格。

S2：任务：核心用例梳理（30 分钟）

基于业务场景列出至少 10 个核心业务用例，并按 P0（必须有）、P1（应该有）、P2（可以有）构建优先级矩阵。该步骤的关键目标是锁定 MVP 范围与需求权重，确保 4 天工期内交付最核心的功能

&#x20;      验收：需确认用例表中至少包含 10 个核心业务用例，每个用例需有清晰的用例名称、用户角色、操作流程与预期结果。所有用例需按 P0（必须有）、P1（应该有）、P2（可以有）三个等级标注优先级，并形成优先级矩阵。其中 P0 用例必须覆盖工单核心流程，包括工单列表查看、工单筛选搜索、工单详情查看、工单状态流转、实时消息通知这五个最核心的场景，不得遗漏。P1 用例应覆盖 Dashboard 概览、多租户切换、国际化切换等进阶功能。用例数量不足 10 个、P0 用例未覆盖核心流程、或优先级标注缺失，均视为验收不通过。

S3:任务：架构决策 ADR（30 分钟）

输出 3 份架构决策记录（Architecture Decision Record），分别覆盖：Monorepo 分层方案、状态管理选型方案、多租户主题系统方案。每份 ADR 需包含背景、决策、备选方案、后果四个部分。该步骤的关键目标是确定分层结构、Repo 模式与技术栈，为后续开发提供决策依据。

&#x20;    验收：确认至少产出 3 份架构决策记录文档，分别对应 Monorepo 分层方案、状态管理选型方案、多租户主题系统方案。每份 ADR 必须包含四个标准部分：背景（说明为什么需要做这个决策、面临什么问题）、决策（明确最终选择了什么方案，如 pnpm + Turborepo、Zustand + TanStack Query、CSS 变量 + Design Token）、备选方案（列出至少 2 个被考虑过但未选择的方案，并说明未选择的原因）、后果（说明该决策带来的正面影响和需要承担的 trade-off）。决策部分必须有明确的技术选型结论，禁止模棱两可；备选方案部分禁止只写一个选项，必须体现对比分析过程。3 份 ADR 均满足上述结构要求且决策理由充分，即为验收合格。

S4：任务：工程基建搭建（30 分钟）

使用 pnpm + Turborepo 初始化 Monorepo 项目骨架，完成 5 个 Apps 与 10 个共享 Packages 的目录结构、依赖配置、路径别名、构建流水线配置。该步骤的关键目标是完成 5 Apps + 10 共享包的初始化，确保各包可独立构建、可互相引用。

&#x20;      验收：在项目根目录执行 pnpm install，确认依赖安装无报错、无冲突；随后执行 pnpm build，确认全量构建一次性通过，5 个 Apps 与 10 个共享 Packages 均能成功构建。目录结构方面，需确认 Monorepo 根目录下存在 apps/ 与 packages/ 两个顶层目录，apps/ 下包含 5 个应用子目录，packages/ 下包含 10 个共享包子目录，每个子目录均有独立的 package.json 且 name 字段符合 workspace 命名规范。路径别名方面，需确认各 Package 之间可通过 @team-portal/xxx 形式互相引用，TypeScript 能正确解析类型，无模块找不到的报错。Turborepo 配置方面，需确认 turbo.json 中 build、dev、test、lint 任务的 dependsOn 配置正确，执行 pnpm dev 可同时启动多个 App。以上命令执行全部通过且目录结构完整，即为验收合格。

S5：任务：设计系统规范（60 分钟）

基于 Radix UI + Tailwind CSS 建立完整的 Design Token 体系，定义颜色（主色 / 辅助色 / 语义色）、间距、圆角、字体、阴影、动效等基础规范，并产出一批基础组件（Button、Input、Modal、Table 等）。该步骤的关键目标是统一 UI 语言与开发约束，为后续业务页面提供可复用的组件底座。

&#x20;      验收：确认 Design Token 体系完整建立，覆盖颜色（主色、辅助色、语义色、中性色）、间距（4px 基准网格体系）、圆角（small/medium/large 三档）、字体（字号、行高、字重）、阴影、动效时长等基础维度，所有 Token 均在 Tailwind 配置中定义并可通过 CSS 变量在运行时访问。基础组件方面，需确认 Button、Input、Modal、Table 四个核心组件已开发完成，且组件样式全部通过 Tailwind 类名引用 Token，代码中不存在硬编码的颜色值（如 #1677ff）或尺寸值（如 padding: 8px）。需通过 ESLint 规则验证禁用硬编码颜色，扫描组件代码无违规。组件渲染验证方面，在页面中引入这四个组件，确认可正常渲染、交互响应正常、样式符合设计规范。Token 体系完整、基础组件可复用且无硬编码值，即为验收合格。

六、常见陷阱

6.1 Monorepo 工程陷阱

路径别名配置错误：tsconfig.json 的 paths 与 pnpm-workspace.yaml 的包名不匹配，导致模块解析失败。对策：初始化时统一用脚本生成别名配置，每个 Package 的 package.json name 字段与导入路径严格对应。

Packages 间循环依赖：A 包依赖 B 包，B 包又依赖 A 包，造成构建死锁或运行时 undefined。对策：绘制依赖关系图，通过 madge 等工具在 CI 中检测循环依赖；共享逻辑下沉到更底层的 common 包。

Turborepo 任务配置不当：turbo.json 的 dependsOn 配置错误导致构建顺序混乱或缓存失效。对策：明确 build、dev、test、lint 的依赖链，善用 ^build 语法。

6.2 多租户主题陷阱

硬编码样式值：组件中直接写 color: #1677ff 而非引用 Token，导致换肤时遗漏。对策：ESLint 规则禁止硬编码颜色，所有样式必须通过 Tailwind 类名或 CSS 变量引用。

主题切换闪烁（FOUC）：运行时切换主题时出现短暂的默认主题闪烁。对策：在 HTML 入口通过内联脚本提前注入主题变量，优先于 CSS 加载。

Token 命名混乱：primary、brand、main 混用，后期维护成本极高。对策：立项阶段即定死命名规范（如 --color-primary-500），并通过 TypeScript 类型约束 Token key。

6.3 虚拟滚动陷阱

动态行高计算错误：工单内容长度不一时，固定行高导致内容截断或滚动位置跳动。对策：使用支持动态行高的虚拟滚动方案（如 @tanstack/react-virtual 的 dynamic 模式），提前测量或估算行高。

滚动位置丢失：筛选 / 搜索后重新渲染，滚动条重置到顶部。对策：维护滚动位置状态，数据更新后按需恢复；key 保持稳定避免组件卸载重建。

大数据内存泄漏：滚动时未正确清理事件监听或缓存，长时间使用后内存持续增长。对策：使用 useEffect 清理函数，限制数据缓存窗口大小。

6.4 WebSocket 实时通知陷阱

连接频繁断连重连：未做心跳保活，网络波动后连接丢失且无自动重连。对策：实现心跳机制（ping/pong）+ 指数退避重连策略，重连后同步离线消息。

消息堆积导致 UI 卡顿：短时间大量消息推送，频繁 setState 引发渲染风暴。对策：消息队列批量处理，使用 requestAnimationFrame 节流更新，或 Zustand 中批量更新。

多标签页消息重复：同一用户打开多个标签页，每个标签都建立 WebSocket 连接，重复消费消息。对策：使用 BroadcastChannel 或 SharedWorker 实现标签页间消息同步，单连接多标签共享。

6.5 RSC 与客户端状态边界陷阱

在 Server Component 中使用客户端 Hooks：在 RSC 中调用 useState/useEffect 导致构建报错。对策：文件顶部明确 "use client" 指令，严格区分 Server/Client Component，交互逻辑下沉到 Client Component。

Zustand 与 TanStack Query 职责混淆：用 Zustand 存服务端数据，导致缓存、失效、重试逻辑全部要自己写。对策：铁律 —— 服务端数据（API 返回）一律走 TanStack Query，客户端 UI 状态（弹窗开关、表单草稿）才走 Zustand。

6.6 性能与测试陷阱

Lighthouse 分数忽高忽低：未在 CI 中固定测试环境，本地跑分与线上差异大。对策：Lighthouse CI 中设置固定 throttling 配置和多次采样取中位数，避免网络波动干扰。

测试覆盖率虚高：只测工具函数，不测组件交互和业务流程，覆盖率数字好看但无实际保障。对策：强制要求核心业务流程有 Playwright E2E 覆盖，组件有交互测试（非仅快照测试）。

TypeScript any 偷偷混入：第三方库类型缺失时图省事写 any，积少成多。对策：ESLint 开启 @typescript-eslint/no-explicit-any 规则，CI 阻断；第三方类型通过 declare module 或类型补丁补充。

6.7 通用避坑建议

立项阶段必须做 POC 验证：不要假设 Monorepo + RSC + 动态主题一定能跑通，先用最小原型验证基建可行性，再投入大规模开发。

用脚本固化初始化流程：手动搭建 Monorepo 容易遗漏配置，将初始化步骤写成脚手架脚本，确保可复现。

每天结束前跑一次全量构建 + 测试：不要等到最后一天才发现依赖冲突或类型错误，早发现早修复。

