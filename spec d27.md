\# 阶段三项目说明书：进阶功能与联调（Day 27）



\## 一、项目目的



\### 1.1 阶段目标

在阶段二已完成核心工单功能的基础上，补齐企业级 SaaS 必备的进阶能力——响应式适配、国际化、数据概览 Dashboard，并完成全链路状态管理联调与端到端测试覆盖。本阶段的核心目标是让项目从"能跑的功能集合"升级为"可交付的企业级产品"，同时通过 E2E 测试为最终上线提供质量保障。



\### 1.2 业务价值

\- \*\*响应式布局\*\*：客服在家用平板、在公司用大屏，界面均需自动适配，覆盖多终端使用场景

\- \*\*国际化（i18n）\*\*：支持 5 种语言，是产品面向海外市场与跨国企业的基础能力

\- \*\*Dashboard 概览\*\*：为客服主管提供数据可视化看板，支撑工单效率监控与团队管理决策

\- \*\*状态联调\*\*：确保 Zustand 与 TanStack Query 边界清晰，避免状态混乱导致的 Bug

\- \*\*E2E 测试\*\*：用自动化测试锁定核心业务流程，防止后续优化阶段引入回归问题



\### 1.3 能力沉淀目标

通过本阶段掌握移动端优先响应式设计、next-intl 国际化方案、Recharts 数据可视化、状态管理边界划分、Playwright E2E 测试编写等技能，这些是中高级前端工程师的必备能力，也是面试中"工程化"与"质量保障"维度的重要谈资。



\---



\## 二、项目功能



\### 2.1 响应式布局体系

\- \*\*断点体系\*\*：定义移动端（<768px）、平板（768px-1024px）、桌面（1024px-1440px）、大屏（>1440px）四个断点

\- \*\*导航自适应\*\*：大屏显示完整侧边栏导航，平板折叠为图标栏，移动端变为底部 Tab 栏或汉堡菜单

\- \*\*表格响应式\*\*：大屏展示完整列（编号/标题/客户/优先级/状态/时间/处理人），平板隐藏次要列，移动端转为卡片式布局

\- \*\*栅格系统\*\*：基于 Tailwind Grid 实现页面布局的响应式重排，Dashboard 卡片在不同断点下自动调整列数

\- \*\*交互适配\*\*：hover 效果在触屏设备上降级为点击态，下拉菜单在移动端改为全屏弹层



\### 2.2 国际化（i18n）

\- \*\*5 种语言支持\*\*：中文（简体）、英文、日文、韩文、繁体中文

\- \*\*文案管理\*\*：按页面/模块组织 JSON 翻译文件，支持嵌套命名空间

\- \*\*语言切换器\*\*：顶部导航栏提供语言切换下拉，切换后实时更新所有文案

\- \*\*日期/数字本地化\*\*：日期格式、数字千分位、相对时间（"3 分钟前"）随语言自动适配

\- \*\*SEO 友好\*\*：基于 next-intl 的 locale 路由（如 `/en/tickets`、`/ja/tickets`），支持 hreflang 标签

\- \*\*状态保持\*\*：语言切换后不丢失当前筛选条件、滚动位置、表单输入



\### 2.3 Dashboard 数据概览页

\- \*\*统计卡片\*\*：今日新增工单、待处理工单、已解决工单、平均响应时间，四个核心指标卡片

\- \*\*趋势图表\*\*：近 7 天工单量趋势折线图，区分新增与解决两条曲线

\- \*\*分布图表\*\*：工单状态分布饼图、优先级分布柱状图、处理人工单量排行条形图

\- \*\*实时刷新\*\*：数据每 30 秒自动刷新，或手动点击刷新按钮，刷新时显示加载态

\- \*\*下钻交互\*\*：点击统计卡片或图表区域，可跳转到对应筛选条件的工单列表页

\- \*\*空态与错误态\*\*：无数据时显示友好空态插画，请求失败时显示错误提示与重试按钮



\### 2.4 状态管理联调

\- \*\*Zustand 客户端状态\*\*：统一管理 UI 状态（侧边栏折叠、筛选条件、搜索关键词、选中工单、通知面板开关、当前语言）

\- \*\*TanStack Query 服务端状态\*\*：统一管理服务端数据（工单列表、工单详情、Dashboard 统计、用户信息、通知列表）

\- \*\*状态边界梳理\*\*：输出一份状态边界文档，明确每个数据/状态的归属，禁止越界管理

\- \*\*跨页面状态同步\*\*：详情页修改工单状态后，列表页对应数据自动更新（通过 queryKey 失效或 setQueryData）

\- \*\*持久化策略\*\*：筛选条件、语言偏好通过 localStorage 持久化，刷新页面后恢复



\### 2.5 Playwright E2E 测试

\- \*\*核心流程覆盖\*\*：工单列表查看 → 筛选搜索 → 进入详情 → 状态流转 → 返回列表，完整链路测试

\- \*\*多场景测试\*\*：多租户主题切换、语言切换、虚拟滚动加载、实时通知接收

\- \*\*断言策略\*\*：不仅断言元素存在，还断言文本内容、属性、URL、视觉状态

\- \*\*测试数据隔离\*\*：每个测试用例使用独立的 Mock 数据，用例间互不影响

\- \*\*CI 集成\*\*：配置 Playwright 在 CI 中运行，生成 HTML 测试报告与失败截图



\---



\## 三、核心约束



\### 3.1 响应式约束

\- 必须覆盖 768px（平板）、1280px（桌面）、1920px（大屏）三个核心断点，无横向滚动条

\- 断点切换时布局重排流畅，无明显跳动或元素闪烁

\- 移动端（模拟 375px 宽度）下所有功能可操作，按钮点击区域 ≥ 44px × 44px

\- 表格在平板下至少保留编号、标题、状态三列，不得出现信息完全丢失



\### 3.2 国际化约束

\- 5 种语言翻译文件完整，核心页面文案覆盖率 100%，无硬编码中文字符

\- 语言切换后日期格式正确（中文 YYYY-MM-DD、英文 MM/DD/YYYY、日文 YYYY/MM/DD）

\- 长文本语言（如英文、德文）下布局不溢出，按钮、表格单元格自适应宽度

\- RTL 语言预留扩展能力（本阶段不实现，但结构上不阻碍后续添加）



\### 3.3 Dashboard 约束

\- 图表首屏加载 ≤ 2s（含数据请求），图表渲染无明显卡顿

\- 数据刷新不闪烁：使用 TanStack Query 的 `placeholderData` 保持旧数据显示，新数据到达后平滑过渡

\- 图表可访问性：每个图表有文本描述（`aria-label`），颜色不仅靠色相区分（加图案或标签）

\- 统计卡片数字有动效（数字滚动），但不影响性能



\### 3.4 代码质量约束

\- TypeScript 严格模式 0 `any`，翻译 key、图表数据、状态 store 均有完整类型

\- E2E 测试至少覆盖 5 条核心业务流程，每条测试有明确的测试名称与断言

\- Zustand store 按领域拆分（uiStore、ticketStore、notificationStore），禁止单个 store 超过 200 行

\- 响应式断点统一在 Tailwind 配置中定义，禁止在组件中硬编码 `max-width: 768px`



\### 3.5 性能约束

\- Dashboard 页面 Lighthouse 性能分 ≥ 90

\- 语言切换耗时 ≤ 100ms，切换过程中无白屏

\- 响应式断点切换（拖拽浏览器窗口）无明显布局抖动



\---



\## 四、技术限制



\### 4.1 响应式技术限制

\- 必须使用 \*\*Tailwind CSS 的响应式前缀\*\*（`sm:`、`md:`、`lg:`、`xl:`）实现断点适配

\- 不得使用 CSS 媒体查询的原生写法（`@media (min-width: 768px)`）散落各组件

\- 断点值必须在 `tailwind.config.ts` 中统一配置，与设计规范一致

\- 移动端优先（Mobile First）策略：默认样式为移动端，大屏通过前缀逐步增强



\### 4.2 国际化技术限制

\- 必须使用 \*\*next-intl\*\* 作为国际化方案，与 Next.js App Router 集成

\- 翻译文件格式为 JSON，按 `messages/{locale}.json` 组织，支持嵌套命名空间

\- 路由结构必须为 `\[locale]` 动态段，如 `app/\[locale]/tickets/page.tsx`

\- 不得使用 `react-i18next` 或其他 i18n 库

\- 语言切换必须通过 `useRouter` + `usePathname` 动态修改路由前缀，而非仅改状态



\### 4.3 图表技术限制

\- 必须使用 \*\*Recharts\*\* 作为图表库，基于 React 组件化声明

\- 不得使用 ECharts、Chart.js 等其他图表库

\- 图表数据必须通过 TanStack Query 获取，禁止在组件中硬编码 Mock 数据

\- 图表容器必须有明确的宽高（响应式容器），不得出现高度为 0 的不可见图表



\### 4.4 状态管理技术限制

\- 客户端状态必须使用 \*\*Zustand\*\*，服务端状态必须使用 \*\*TanStack Query\*\*

\- 禁止使用 `useState` + `useContext` 管理应全局共享的状态

\- 禁止用 Zustand 存储服务端返回的数据（如工单列表、用户信息）

\- TanStack Query 的 queryKey 必须遵循 `\[实体, 操作, 参数]` 的命名规范

\- Zustand store 必须使用 TypeScript 定义 State 接口，禁止隐式类型推断



\### 4.5 E2E 测试技术限制

\- 必须使用 \*\*Playwright\*\* 作为 E2E 测试框架

\- 测试文件放在 `apps/web/e2e/` 目录下，命名为 `\*.spec.ts`

\- 测试运行在 `localhost:3000`，需先启动开发服务器或构建后预览

\- 断言使用 Playwright 内置的 `expect`，不得使用 Jest 的断言

\- 选择器优先使用 `getByRole`、`getByLabel`、`getByText`，禁止使用 CSS 类名选择器（易随样式变更而失效）



\---



\## 五、完成步骤（含验收要求）



\### 步骤 01：响应式布局体系搭建（约 1.5 小时）



首先在 `tailwind.config.ts` 中统一配置四个断点（sm/md/lg/xl），与设计规范对齐。然后改造全局布局组件 `AppLayout`：大屏显示完整侧边栏（含图标+文字），平板宽度时侧边栏折叠为仅图标栏，移动端时侧边栏变为顶部汉堡菜单或底部 Tab 栏。接着改造工单列表页的表格组件：大屏展示全部 7 列，平板隐藏处理人和时间列，移动端将表格转为卡片式列表（每张卡片展示工单标题、状态标签、优先级、时间）。然后为 Dashboard 页面配置响应式栅格：大屏 4 列统计卡片 + 2 列图表，平板 2 列统计卡片 + 1 列图表，移动端 1 列全堆叠。最后在三个核心断点（768px、1280px、1920px）下逐页验证布局，修复溢出、重叠、不可点击等问题。



\*\*验收要求：\*\*

需确认 `tailwind.config.ts` 中断点配置完整且与设计规范一致，`screens` 字段至少包含 `sm: 640px`、`md: 768px`、`lg: 1024px`、`xl: 1280px`、`2xl: 1536px`，项目中无组件使用原生 `@media` 写死断点。`AppLayout` 组件需在三个断点下表现正确：1920px 大屏侧边栏完整展开（图标+文字+导航名称），1280px 桌面端侧边栏可折叠为图标栏且有折叠/展开按钮，768px 平板端侧边栏默认折叠为图标栏，移动端（375px）导航变为顶部汉堡菜单或底部 Tab 栏，三种模式切换流畅无闪烁。工单列表在 1920px 下展示完整 7 列（编号/标题/客户/优先级/状态/时间/处理人），768px 下至少保留编号/标题/状态三列且其余列可通过横向滚动或展开查看，375px 下转为卡片式布局（每张卡片含标题、状态、优先级、时间），无横向滚动条。Dashboard 统计卡片在 1920px 下 4 列并排、1280px 下 2 列并排、768px 下 2 列并排、375px 下单列堆叠，图表区域相应调整为 2 列/1 列。所有断点下无元素重叠、无内容溢出、无横向滚动条（表格内部滚动除外），移动端按钮点击区域 ≥ 44px×44px。拖拽浏览器窗口在断点间切换时，布局重排流畅无明显跳动。若任一断点下出现横向滚动条、元素重叠或功能不可操作，即视为验收不通过。



\---



\### 步骤 02：国际化（i18n）集成（约 1.5 小时）



首先安装并配置 `next-intl`，在 `app/\[locale]` 目录下重构路由结构，将现有页面移入 `\[locale]` 动态段下。然后创建 5 个语言的翻译文件（`messages/zh.json`、`en.json`、`ja.json`、`ko.json`、`zh-TW.json`），按页面模块组织嵌套命名空间（如 `tickets.list.title`、`dashboard.stats.total`）。接着封装 `useTranslations` Hook 的使用规范，在所有页面和组件中替换硬编码文案。然后实现语言切换器组件：放在顶部导航栏，点击展开语言列表，选择后通过 `useRouter` 修改路由前缀（如从 `/zh/tickets` 跳转到 `/en/tickets`），并保持当前路径和查询参数。接着处理日期和数字本地化：使用 `next-intl` 的 `useFormatter` 或 `Intl.DateTimeFormat`/`Intl.NumberFormat`，确保日期、相对时间、数字千分位随语言变化。最后逐页检查 5 种语言下的文案显示，修复翻译缺失、布局溢出、日期格式错误等问题。



\*\*验收要求：\*\*

需确认项目路由结构已重构为 `app/\[locale]/` 动态段模式，访问 `/tickets` 自动重定向到默认语言（如 `/zh/tickets`），访问 `/en/tickets`、`/ja/tickets` 等路径均能正确渲染对应语言页面。5 个翻译文件（zh、en、ja、ko、zh-TW）均存在且核心页面文案覆盖率 100%，通过全局搜索确认代码中无硬编码中文字符串（注释除外），所有用户可见文案均通过 `useTranslations('namespace')` 获取。语言切换器需位于顶部导航栏，支持 5 种语言切换，切换后 URL 前缀同步变更（如 `/zh/tickets` → `/en/tickets`），当前路径和 query 参数保持不变，页面文案实时更新无白屏，切换耗时 ≤ 100ms。日期格式需验证：中文显示 `2026-08-19`，英文显示 `Aug 19, 2026` 或 `08/19/2026`，日文显示 `2026/08/19`，相对时间（"3 分钟前"）在各语言下正确翻译。长文本场景需验证：英文翻译比中文长时，按钮、表格单元格、导航项自适应宽度，不出现文字截断或布局溢出。语言切换后当前筛选条件、搜索关键词、滚动位置保持不变，不重置。SEO 方面需确认页面 `<html lang>` 属性随语言变化，且包含 `hreflang` 链接标签。若存在硬编码文案、语言切换后状态丢失、或某语言下布局溢出，即视为验收不通过。



\---



\### 步骤 03：Dashboard 数据概览页开发（约 2 小时）



首先创建 Dashboard 页面路由 `app/\[locale]/dashboard/page.tsx`，通过 RSC 首屏加载统计数据。然后开发四个统计卡片组件：今日新增工单、待处理工单、已解决工单、平均响应时间，每个卡片包含图标、数值、同比变化（上升/下降箭头+百分比），数值有滚动动效。接着基于 Recharts 开发三个图表：近 7 天工单量趋势折线图（新增 vs 解决双曲线）、工单状态分布饼图（待处理/处理中/已解决/已关闭四色）、处理人工单量排行条形图（Top 5）。然后集成 TanStack Query 实现数据获取与 30 秒自动刷新（`refetchInterval: 30000`），刷新时使用 `placeholderData` 保持旧数据显示，避免闪烁。接着实现下钻交互：点击"待处理工单"卡片跳转到工单列表页并自动应用状态筛选，点击趋势图某一天跳转到该日期的工单列表。最后处理空态和错误态：无数据时显示友好插画+提示文字，请求失败时显示错误信息+重试按钮。



\*\*验收要求：\*\*

需确认 Dashboard 页面路由为 `/\[locale]/dashboard`，可从侧边栏导航进入。四个统计卡片需完整展示：今日新增、待处理、已解决、平均响应时间，每个卡片有对应图标、数值、同比变化指示（上升绿色↑/下降红色↓+百分比），数值加载时有滚动动效，加载完成后显示最终值。三个图表需基于 Recharts 渲染：折线图展示近 7 天新增与解决两条曲线，有图例、坐标轴、tooltip；饼图展示四种状态分布，有百分比标签和图例；条形图展示处理人 Top 5 排行，有坐标轴和数值标签。数据获取需通过 TanStack Query，queryKey 为 `\['dashboard', 'stats', locale]`，设置 `refetchInterval: 30000` 自动刷新，刷新时旧数据保持显示（`placeholderData: keepPreviousData`），新数据到达后平滑更新无闪烁。下钻交互需验证：点击"待处理工单"卡片跳转到 `/tickets?status=pending`，点击趋势图的某一天数据点跳转到 `/tickets?date=YYYY-MM-DD`，筛选条件正确应用。空态需验证：Mock 无数据时显示友好空态插画和提示文字（如"暂无工单数据"），不显示空白或报错。错误态需验证：Mock 请求失败时显示错误信息和"重试"按钮，点击重试后重新请求。可访问性方面，每个图表有 `aria-label` 描述，颜色不仅靠色相区分（饼图加百分比标签，折线图加不同线型或数据点标记）。性能方面，Dashboard 首屏加载 ≤ 2s，图表渲染无卡顿，Lighthouse 性能分 ≥ 90。若图表使用硬编码数据、刷新时闪烁、或下钻跳转筛选条件不正确，即视为验收不通过。



\---



\### 步骤 04：状态管理联调与边界梳理（约 1 小时）



首先梳理现有代码中的所有状态，按"客户端 UI 状态"和"服务端数据状态"分类，输出一份状态边界文档。然后重构 Zustand store：按领域拆分为 `uiStore`（侧边栏折叠、通知面板开关、当前语言）、`ticketStore`（筛选条件、搜索关键词、选中工单 ID、滚动位置）、`notificationStore`（未读计数、通知列表），每个 store 有独立的 State 接口和 actions。接着统一 TanStack Query 的 queryKey 命名规范：`\['tickets', 'list', filters]`、`\['tickets', 'detail', id]`、`\['dashboard', 'stats']`、`\['notifications', 'list']`，并配置全局默认选项（`staleTime: 60000`、`retry: 2`）。然后实现跨页面状态同步：详情页修改工单状态后，调用 `queryClient.invalidateQueries({ queryKey: \['tickets', 'list'] })` 或 `queryClient.setQueryData` 精确更新列表中的对应项，确保返回列表时数据已更新。最后实现持久化：使用 `zustand/middleware` 的 `persist` 中间件，将筛选条件、语言偏好、侧边栏状态存入 localStorage，刷新页面后自动恢复。



\*\*验收要求：\*\*

需确认已输出一份状态边界文档，明确列出每个状态的名称、类型、归属（Zustand/TanStack Query/组件本地 state）、使用页面，且归属判断正确（服务端数据全部归 TanStack Query，UI 交互状态全部归 Zustand，组件内部临时状态归 useState）。Zustand store 需按领域拆分为至少 3 个独立 store（uiStore、ticketStore、notificationStore），每个 store 有明确的 TypeScript State 接口，代码中无单个 store 超过 200 行的情况，无 `any` 类型。TanStack Query 的 queryKey 需统一遵循 `\[实体, 操作, 参数]` 规范，全局 `QueryClient` 配置了合理的默认选项（`staleTime`、`retry`、`refetchOnWindowFocus`），代码中无散落的独立 QueryClient 实例。跨页面同步需验证：在详情页将工单状态从"待处理"改为"已解决"，返回列表页后该工单状态立即显示为"已解决"，无需手动刷新（通过 invalidateQueries 或 setQueryData 实现）。持久化需验证：设置筛选条件（如状态=待处理、优先级=高）后刷新页面，筛选条件自动恢复；切换语言为英文后刷新页面，语言保持为英文；折叠侧边栏后刷新页面，侧边栏保持折叠状态。状态边界需验证：代码中无 Zustand store 存储服务端返回的工单数据或用户信息，无 TanStack Query 管理侧边栏折叠等纯 UI 状态，无 `useContext` 全局状态。若存在状态归属错误、跨页面数据不同步、或刷新后状态丢失，即视为验收不通过。



\---



\### 步骤 05：Playwright E2E 测试编写（约 1 小时）



首先安装并初始化 Playwright，配置 `playwright.config.ts`（测试目录、基础 URL `http://localhost:3000`、浏览器 Chromium、超时时间）。然后编写 5 条核心 E2E 测试用例：用例 1「工单列表浏览与筛选」——访问列表页，验证工单数据加载，应用状态筛选，验证列表内容更新；用例 2「工单详情与状态流转」——从列表点击进入详情，验证详情信息，执行状态流转操作，验证状态更新；用例 3「多租户主题切换」——切换租户主题，验证界面配色变化；用例 4「语言切换」——切换为英文，验证页面文案变为英文且 URL 前缀更新；用例 5「Dashboard 数据加载」——访问 Dashboard，验证统计卡片和图表正常渲染。每条测试使用 `getByRole`、`getByLabel`、`getByText` 等语义化选择器，断言包含元素可见性、文本内容、URL 变化。最后配置测试运行脚本，在本地启动应用后执行测试，确保全部通过，生成 HTML 报告。



\*\*验收要求：\*\*

需确认 Playwright 已正确安装配置，`playwright.config.ts` 中 `testDir` 指向 `apps/web/e2e`，`use.baseURL` 为 `http://localhost:3000`，配置了至少 Chromium 浏览器，超时时间合理（如 30s）。测试用例数量 ≥ 5 条，每条有清晰的 `test('描述')` 名称，覆盖：工单列表筛选、工单详情状态流转、主题切换、语言切换、Dashboard 加载五个核心场景。选择器规范需验证：测试代码中使用 `page.getByRole()`、`page.getByLabel()`、`page.getByText()`、`page.getByPlaceholder()` 等语义化选择器，禁止使用 `.className`、`#id` 等脆弱选择器（通过代码审查确认）。断言质量需验证：每条测试至少有 2 个断言，不仅断言元素存在（`toBeVisible()`），还断言文本内容（`toHaveText()`）、URL（`toHaveURL()`）、属性（`toHaveAttribute()`）等具体行为。测试隔离需验证：每条测试独立运行，不依赖前一条测试的状态，使用 `test.beforeEach` 导航到起始页面，Mock 数据不互相污染。运行结果需验证：执行 `pnpm test:e2e` 后 5 条测试全部通过（绿色），无 flaky 测试（连续运行 3 次均通过），失败时自动生成截图和 trace。CI 配置需验证：项目中有 E2E 测试的 npm script，`playwright.config.ts` 中配置了 `reporter: \[\['html', { open: 'never' }]]` 生成 HTML 报告。若测试用例不足 5 条、使用脆弱选择器、或存在失败/flaky 测试，即视为验收不通过。



\---



\## 六、常见陷阱



\### 6.1 响应式布局陷阱

\- \*\*断点设计混乱\*\*：`sm/md/lg` 的具体像素值在 Tailwind 默认配置与设计稿之间不一致，导致实现与设计偏差。\*\*对策\*\*：在 `tailwind.config.ts` 中统一覆盖 `screens` 配置，与设计规范严格对齐，全项目使用同一套断点，禁止组件内自行定义媒体查询。

\- \*\*移动端表格处理不当\*\*：简单地用 `overflow-x: auto` 让表格横向滚动，在移动端体验极差（需要左右滑动看全信息）。\*\*对策\*\*：移动端将表格转为卡片式布局（`hidden md:table` + `md:hidden block`），每张卡片展示关键字段，而非让用户横向滚动。

\- \*\*断点切换时布局跳动\*\*：侧边栏从展开变折叠时，主内容区宽度突变导致内容重排跳动。\*\*对策\*\*：侧边栏折叠/展开加 CSS transition（宽度 0.2s ease），主内容区使用 `flex: 1` 自适应，避免固定宽度计算。

\- \*\*hover 在触屏设备上的残留态\*\*：移动端点击按钮后 hover 样式残留，直到点击其他区域才消失。\*\*对策\*\*：使用 `@media (hover: hover)` 媒体查询包裹 hover 样式，或用 Tailwind 的 `hover:` 仅在支持 hover 的设备上生效。

\- \*\*100vh 在移动端浏览器的问题\*\*：移动端浏览器地址栏伸缩导致 `100vh` 高度计算错误，出现底部被遮挡或滚动条异常。\*\*对策\*\*：使用 `100dvh`（动态视口高度）或 `min-h-screen`，并为不支持的浏览器提供 `100vh` 降级。



\### 6.2 国际化陷阱

\- \*\*硬编码文案遗漏\*\*：组件中散落着大量硬编码中文字符串，逐页替换时容易遗漏（尤其是 placeholder、tooltip、错误提示、确认弹窗文案）。\*\*对策\*\*：配置 ESLint 规则（如 `eslint-plugin-i18next` 的 `no-literal-string`）自动检测硬编码字符串，CI 中阻断；开发时养成"写文案先加翻译 key"的习惯。

\- \*\*翻译 key 命名混乱\*\*：`title`、`name`、`label` 等通用 key 在不同命名空间下重复，维护时找不到对应位置。\*\*对策\*\*：按页面/模块建立嵌套命名空间（如 `tickets.list.status.pending`、`tickets.detail.actions.close`），key 名语义化，禁止扁平结构。

\- \*\*英文长文本导致布局溢出\*\*：中文 4 个字的按钮，英文可能是 15 个字符，固定宽度的按钮出现文字截断或换行。\*\*对策\*\*：按钮、标签使用 `min-width` 而非固定 `width`，表格列宽使用 `min-w` 自适应；在翻译文件中对长文本做适当缩写（如 "Settings" → "Settings" 而非 "System Configuration Settings"）。

\- \*\*语言切换后状态丢失\*\*：通过 `router.push` 切换语言时，页面重新加载导致 Zustand 状态重置，筛选条件丢失。\*\*对策\*\*：将需要持久化的状态（筛选条件、语言偏好）存入 localStorage，语言切换后从 localStorage 恢复；或使用 `next-intl` 的 `useRouter` 配合 `usePathname` 做客户端导航，避免全页刷新。

\- \*\*日期/数字格式硬编码\*\*：代码中直接用 `date.toLocaleDateString('zh-CN')` 写死中文格式，切换语言后不变化。\*\*对策\*\*：使用 `next-intl` 提供的 `useFormatter` 或 `Intl.DateTimeFormat`，locale 参数从当前语言动态获取，禁止写死。



\### 6.3 Dashboard 图表陷阱

\- \*\*图表容器高度为 0\*\*：Recharts 的 `ResponsiveContainer` 需要父元素有明确高度，否则图表不显示且不报错，排查困难。\*\*对策\*\*：给图表外层容器设置明确的 `height`（如 `h-80`）或使用 `aspect-ratio`，开发时先用固定高度验证，再改为响应式。

\- \*\*数据刷新时闪烁\*\*：30 秒自动刷新时，旧数据消失→加载态→新数据出现，用户看到明显闪烁。\*\*对策\*\*：使用 TanStack Query 的 `placeholderData: keepPreviousData`，刷新期间保持旧数据显示，新数据到达后直接替换，无加载态闪烁。

\- \*\*图表无数据时崩溃\*\*：传入空数组 `\[]` 给饼图或折线图，Recharts 渲染异常或显示空白。\*\*对策\*\*：在图表组件外层判断数据是否为空，空时显示空态组件而非渲染图表；给图表设置 `margin` 和默认坐标轴，避免边界情况。

\- \*\*颜色仅靠色相区分\*\*：饼图四种状态用四种颜色，色盲用户无法区分，且打印时灰度下难以辨别。\*\*对策\*\*：饼图加百分比标签和图例文字，折线图用不同线型（实线/虚线）或数据点形状（圆形/方形）辅助区分，不仅依赖颜色。

\- \*\*图表重渲染导致性能问题\*\*：父组件状态变化（如侧边栏折叠）导致图表频繁重绘，出现卡顿。\*\*对策\*\*：将图表组件用 `React.memo` 包裹，仅在数据变化时重渲染；图表数据用 `useMemo` 缓存，避免每次渲染生成新引用。



\### 6.4 状态管理联调陷阱

\- \*\*Zustand 与 TanStack Query 职责混淆\*\*：图省事把工单列表数据同时存在 Zustand 里，导致两套数据源不同步，列表显示旧数据。\*\*对策\*\*：铁律——服务端数据（API 返回的）只走 TanStack Query，Zustand 只存 UI 交互状态；代码审查时重点检查 store 中是否有服务端数据。

\- \*\*queryKey 设计不当导致缓存污染\*\*：`\['tickets', 'list']` 不带筛选参数，切换筛选后复用了旧缓存，显示错误数据。\*\*对策\*\*：queryKey 必须包含所有影响响应的参数（筛选、排序、分页、语言），参数变了 key 就变，缓存自然隔离。

\- \*\*invalidateQueries 范围过大\*\*：修改一个工单后 `invalidateQueries({ queryKey: \['tickets'] })` 导致所有工单相关查询（列表、详情、统计）全部重新请求，浪费资源。\*\*对策\*\*：精确失效——只失效受影响的查询（如 `\['tickets', 'list']`），或用 `setQueryData` 直接更新缓存中的对应项，避免重新请求。

\- \*\*persist 中间件存储敏感信息\*\*：不小心把用户 token 或个人信息存入 localStorage，存在安全风险。\*\*对策\*\*：persist 时用 `partialize` 配置只持久化非敏感的 UI 状态（筛选条件、语言偏好），明确排除用户信息、token 等敏感数据。

\- \*\*store 过于庞大\*\*：所有状态塞在一个 `useStore` 里，任何一个状态变化导致所有使用 store 的组件重渲染。\*\*对策\*\*：按领域拆分 store（ui/ticket/notification），并使用 `useShallow` 或精确选择器（`useStore(s => s.sidebarCollapsed)`）避免不必要的重渲染。



\### 6.5 Playwright E2E 测试陷阱

\- \*\*使用脆弱的 CSS 选择器\*\*：`page.click('.btn-primary')` 或 `page.click('#submit-button')`，样式重构或类名变更后测试全部失效。\*\*对策\*\*：强制使用语义化选择器 `getByRole('button', { name: '提交' })`、`getByLabel('搜索')`、`getByText('工单列表')`，既稳定又提升可访问性。

\- \*\*测试依赖前一条测试的状态\*\*：用例 1 登录后，用例 2 直接假设已登录，单独运行用例 2 就失败。\*\*对策\*\*：每条测试独立，用 `test.beforeEach` 做导航和必要的前置操作（如登录），测试间不共享状态；需要登录态时用 `storageState` 复用但不依赖顺序。

\- \*\*等待时间写死\*\*：`await page.waitForTimeout(2000)` 等固定时间，网络慢时不够用导致 flaky，网络快时浪费时间。\*\*对策\*\*：使用 Playwright 的自动等待（`toBeVisible()`、`toHaveText()` 等断言自带等待），或用 `waitForResponse` 等待具体 API 响应，禁止 `waitForTimeout`。

\- \*\*Mock 数据与真实接口不一致\*\*：E2E 测试用 Mock 数据全绿，上线后真实接口字段变更导致 Bug。\*\*对策\*\*：Mock 数据结构严格对齐真实 API 响应（可用 TypeScript 类型约束），或在 CI 中对接测试环境真实接口做冒烟测试；定期同步接口文档。

\- \*\*测试运行环境不一致\*\*：本地测试通过，CI 中因分辨率、时区、字体差异失败。\*\*对策\*\*：在 `playwright.config.ts` 中固定 `viewport`（如 1280×720）、`timezoneId`（如 `Asia/Shanghai`），确保本地与 CI 环境一致；使用 Docker 容器运行测试进一步统一环境。



\### 6.6 通用避坑建议

\- \*\*响应式和国际化尽早介入\*\*：不要等功能全部做完才加响应式和 i18n，那时组件结构可能不支持，重构成本极高。本阶段集中处理时，若发现阶段二的组件结构不利于适配，果断重构而非打补丁。

\- \*\*Dashboard 先定数据结构再画图\*\*：先和后端（或 Mock）确认统计接口返回的数据结构，再基于结构开发图表组件，避免图表写好后数据对不上导致返工。

\- \*\*状态联调用 DevTools 验证\*\*：安装 Redux DevTools（Zustand 支持）和 TanStack Query DevTools，实时观察状态变化和缓存情况，比 console.log 高效得多。

\- \*\*E2E 测试先写最核心的流程\*\*：1 小时写不完所有测试，优先保证"工单列表→详情→状态流转→返回"这条主链路稳定，其他场景可在阶段四补充。

\- \*\*每天结束前全量构建+测试\*\*：本阶段引入了 i18n 路由重构和 E2E 测试，改动较大，结束前务必跑一次 `pnpm build` + `pnpm test:e2e`，早发现问题早修复。





